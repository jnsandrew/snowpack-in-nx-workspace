{
    "$schema": "http://json-schema.org/schema",
    "type": "object",
    "cli": "nx",
    "properties": {
        "noCache": {
            "type": "boolean",
            "description": "Clear the Snowpack cache before running?"
        }
    }
}